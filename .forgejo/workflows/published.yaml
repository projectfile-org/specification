# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by ci-resolver from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target forgejo
# A freshness gate (pf-ci generate --check) runs in CI so this file cannot
# silently drift from the source DAG.
---
name: published
"on":
  push: {}
  pull_request: {}
  workflow_dispatch:
env:
  M6E_TAG: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
  M6E_RUN_SCOPE: ${{ github.run_id }}-${{ github.run_attempt }}
jobs:
  artifact-ready:
    runs-on: docker
    needs: ["built"]
    steps:
      - run: echo artifact-ready
  built:
    runs-on: docker
    needs: ["source-is-ready"]
    steps:
      - run: echo built
  projectfile-is-synced:
    runs-on: docker
    needs: ["projectfile-is-valid"]
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
      - name: pf-bridge-check
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.PF_BRIDGE_IMAGE || format('{0}/projectfile/bridge', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: pf-bridge check
  projectfile-is-valid:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
      - name: pf-validate
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.PF_CLI_IMAGE || format('{0}/projectfile/cli', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: pf-cli validate
  published:
    runs-on: docker
    needs: ["ready-to-publish"]
    steps:
      - run: echo published
  ready-to-publish:
    runs-on: docker
    needs: ["artifact-ready", "source-is-compliant", "source-is-secure"]
    steps:
      - run: echo ready-to-publish
  source-is-compliant:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
      - name: reuse-lint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: reuse lint
  source-is-ready:
    runs-on: docker
    needs: ["projectfile-is-synced", "source-is-tested", "source-passes-lint"]
    steps:
      - run: echo source-is-ready
  source-is-secure:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
      - name: grype-scan-source
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: auto-grype
          mounts: /app/data/ci-cache/grype-db:/app/.cache/grype:ro
      - name: trivy-scan-source
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: auto-trivy fs
          env: |-
            TRIVY_CACHE_BACKEND
          mounts: /app/data/ci-cache/trivy-db:/app/.cache/trivy:ro
      - uses: actions/upload-artifact@v3
        if: ${{ always() }}
        with:
          name: reports-source-is-secure-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
  source-is-tested:
    runs-on: docker
    steps:
      - run: echo source-is-tested
  source-passes-lint:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true
      - name: cffr-validate
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_R_TOOLS_IMAGE || format('{0}/d9t/r-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cffr-validate
      - name: jsonlint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: auto-jsonlint
      - name: yamllint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'kiota.ch') }}
          version: ${{ env.M6E_TAG }}
          run: auto-yamllint
