---
# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: CC-BY-4.0
# SPDX-License-Identifier: MIT

# Shape: org.projectfile.health extension namespace
# Illustrative fragment — not a complete projectfile document.
# See registry.yaml for ownership and status.

org:
  projectfile:
    health:
      endpoints:
        - path: /health  # REQUIRED  URL path
          port: 8080  # OPTIONAL  TCP port (1-65535)
          protocol: http  # OPTIONAL  http | https | tcp | grpc (default: http)
          kind: liveness  # OPTIONAL  liveness | readiness | startup
        - path: /ready
          port: 8080
          protocol: http
          kind: readiness
