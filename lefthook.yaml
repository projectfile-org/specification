# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by pf-ci from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target lefthook
# Each hook dispatches to the DAG node of the same name (`make <stage>`); the
# node's tool closure is resolved at run time, so the DAG stays the single source
# of truth and this file changes only when a hook node is added or removed, never
# when its membership does.
---
pre-commit:
  commands:
    dag:
      run: make pre-commit
pre-push:
  commands:
    dag:
      run: make pre-push
